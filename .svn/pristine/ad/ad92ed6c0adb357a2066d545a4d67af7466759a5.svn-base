import asset from './opsManage/asset'
import device from './opsManage/device'
import log from './opsManage/log'
import video from './opsManage/video'

const state = {
  ...asset.state,
  ...device.state,
  ...log.state,
  ...video.state
}
const getters = {
  ...asset.getters,
  ...device.getters,
  ...log.getters,
  ...video.getters
}
const mutations = {
  ...asset.mutations,
  ...device.mutations,
  ...log.mutations,
  ...video.mutations
}

const actions = {
  ...asset.actions,
  ...device.actions,
  ...log.actions,
  ...video.actions
}

export default {
  state,
  mutations,
  actions,
  getters
}
