
// const oneDay = 24 * 3600 * 1000
/* const fiveMinutes = 3000
let startTime = +new Date() */
// 硬盘存储信息
/*const dataX = []
const dataY = []
const memoryData = [] // 内存存储信息
const CpuData = [] // cup信息
let refreshTime = +new Date() - 3000
let refreshMemoryTime = +new Date() - 3000
let refreshCPUTime = +new Date() - 3000*/
// 把new Date出来的时间格式转换为data123中的日期格式
export function DateFormat(now) {
  return now.toLocaleDateString() + ' ' + now.getHours() + now.toLocaleTimeString().substr(-6, 6)
}
// 用来返回data123一样的数据格式
/* function chartData(i, data, now) {
  return {
    value: [
      DateFormat(now),
      data[i] / 1024
    ]
  }
} */

// var data123 = [
//     { value: [ '2016/2/18 03:00:00', 363 ] },
//     { value: [ '2016/2/18 04:00:00', 384 ] },
//     { value: [ '2016/2/18 05:00:00', 371 ] },
// ];
// 把原数据改成data123一样的格式
/* function changeData(datain) {
  let liuru = []

  /!* for (let i = 0; i < datain.length; i++) {
    startTime = new Date(+startTime + fiveMinutes)
    liuru.push(chartData(i, datain, startTime))
  } *!/
  // console.log(liuru)
  return liuru
} */

// eslint-disable-next-line no-unused-vars
/*export function refresh(data_in, date) {
  refreshTime += 3000
  if (dataX.length === 10) { // 限制10个
    dataX.shift() // value: [DateFormat(new Date(refreshTime)), data_in.value / 1024]
    dataY.shift()
    dataX.push(DateFormat(date))
    dataY.push(data_in.value / 1024)
  } else {
    dataX.push(DateFormat(date))
    dataY.push(data_in.value / 1024)
  }
  console.log({Xvalue: dataX, Yvalue: dataY})
  return {Xvalue: dataX, Yvalue: dataY}
}
export function refreshMemory(data_in) {
  refreshMemoryTime += 3000
  if (memoryData.length === 10) {
    memoryData.shift()
    memoryData.push({value: [DateFormat(new Date(refreshMemoryTime)), data_in.value / 1024 / 1024]})
  } else {
    memoryData.push({value: [DateFormat(new Date(refreshMemoryTime)), data_in.value / 1024 / 1024]})
  }
  return memoryData
}
export function refreshCPU(data_in) {
  refreshCPUTime += 3000
  if (CpuData.length === 10) {
    CpuData.shift()
    CpuData.push({value: [DateFormat(new Date(refreshCPUTime)), data_in.value / 1024]})
  } else {
    CpuData.push({value: [DateFormat(new Date(refreshCPUTime)), data_in.value / 1024]})
  }

  return CpuData
}*/
