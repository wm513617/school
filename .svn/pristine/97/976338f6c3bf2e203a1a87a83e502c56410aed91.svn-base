import controlManage from './videoStructured/controlManage'
import captureRealtime from './videoStructured/captureRealtime'
import alarmSearch from './videoStructured/alarmSearch'
const state = {
  ...controlManage.state,
  ...captureRealtime.state,
  ...alarmSearch.state
}
const getters = {
  ...controlManage.getters,
  ...captureRealtime.getters,
  ...alarmSearch.getters
}
const mutations = {
  ...controlManage.mutations,
  ...captureRealtime.mutations,
  ...alarmSearch.mutations
}

const actions = {
  ...controlManage.actions,
  ...captureRealtime.actions,
  ...alarmSearch.actions
}

export default {
  state,
  mutations,
  actions,
  getters
}
