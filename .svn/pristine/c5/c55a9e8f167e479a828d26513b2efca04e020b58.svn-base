/**
 * 消防配置模型
 * **/
'use strict'
const mongoose = require('mongoose')
const Schema = mongoose.Schema

const FireConfSchem = new Schema({
  ismap: {
    type: Boolean, // 应用模式主要显示区 (地图、视频)
    default: true
  }
})
mongoose.model('FireConf', FireConfSchem)
