import configure from './vehicle/configure'
import recognize from './vehicle/recognize'
const state = {
  ...configure.state,
  ...recognize.state
}
const getters = {
  ...configure.getters,
  ...recognize.getters
}
const mutations = {
  ...configure.mutations,
  ...recognize.mutations
}

const actions = {
  ...configure.actions,
  ...recognize.actions
}

export default {
  state,
  mutations,
  actions,
  getters
}
